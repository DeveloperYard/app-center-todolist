import React from 'react';

function UsersInfo({ color, name, isSpecial }) {
  return (
    <div style={{ color }}>
      
    </div>
  );
}

Hello.defaultProps = {
  name: '이름없음'
}

export default UsersInfo;